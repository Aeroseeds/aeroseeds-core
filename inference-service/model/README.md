Place `convnext_tiny_best.pt` in this folder. The inference service loads it at startup from `inference-service/model/convnext_tiny_best.pt`. The checkpoint contains both the model weights and the class names.

The file is stored with Git LFS, so a normal `git clone` fetches it automatically as long as `git-lfs` is installed.
